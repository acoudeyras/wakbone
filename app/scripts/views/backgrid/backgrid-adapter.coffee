define ['./filter-header-cell', 'backgrid'], (FilterHeaderCell) ->

  class BackgridAdapter
    constructor: ({@collection, @columns}) ->
    buildAllOptions: ->
    buildPagingFooter: ->
    buildCols: ->
    buildGrid: ->

